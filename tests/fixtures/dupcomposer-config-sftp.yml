backup_groups:
  sftp_backup:
    encryption:
      enabled: no
    backup_provider:
      url: sftp://sftpuser@myhost.example.com
      password: xxxxxx
    volume_size: 200
    sources:
      /var/www/html:
        backup_path: /home/bkup
        restore_path: /root/restored
